请对 `security_workflow/03_detailed_design.md` 做最终交付检查，并执行以下动作：

1. 检查各章节是否与 `01_constraints.md` 和 `02_baseline.md` 一致
2. 检查每章是否都有 Mermaid 架构图和时序图
3. 检查是否存在“原始资料未支撑却写成 [CONFIRMED]”的内容
4. 输出修订后的 `security_workflow/03_detailed_design.md`
5. 如有新增输入，生成 `security_workflow/04_change_impact.md`

检查项：
- 约束一致性
- 章节一致性
- PDF 导出友好性
- 表格完整性
- 结构体完整性
