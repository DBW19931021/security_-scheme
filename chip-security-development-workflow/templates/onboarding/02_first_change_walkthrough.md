# First Change Walkthrough

Read one OpenSpec change in this order:

```text
proposal → spec → design → tasks → evidence → retrospective → code/tests
```

Run the smallest verification command recorded by the change before claiming
onboarding completion.
