# Implementation Design Shards

Implementation design translates approved architecture into fields, interfaces,
state machines, error handling, and testable rules.

- Shards MUST bind to constraints and decisions.
- Shards MUST NOT create hidden architecture policy.
- The project MUST identify the complete code-landing design source.
