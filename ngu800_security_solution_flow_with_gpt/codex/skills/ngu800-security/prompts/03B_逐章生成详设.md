基于以下输入逐章生成详设：
- `security_workflow/01_constraints.md`
- `security_workflow/02_baseline.md`
- `security_workflow/03_detailed_design/00_chapter_plan.md`
- `security_inputs/inputs_manifest.md`

变更门禁：
- 如果本任务是在既有安全方案基础上做修改，且影响两个以上文件、影响 baseline、或影响 boot/key/cert/attestation/debug/interface/manufacturing 任一安全主路径，必须先生成 Change Request。
- 未生成 CR 前，不得直接修改既有方案正文。

输出目录：
- `security_workflow/03_detailed_design/`

要求：
1. 不要一次性写成单个长文档
2. 每章必须包含：
   - 生效约束 ID
   - 生效 baseline 决策
   - Mermaid 架构图
   - Mermaid 时序图
   - 表格 / 结构体 / 位图（如有依据）
   - 对实现层的影响
   - 冻结项
3. 不允许保留空章节
4. 不允许模板残留
