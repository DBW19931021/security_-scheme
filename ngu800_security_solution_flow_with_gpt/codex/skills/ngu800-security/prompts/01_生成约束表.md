请先不要直接生成完整安全方案。

请严格按以下步骤执行，并把结果写入 `security_workflow/01_constraints.md`：

1. 先阅读 `security_inputs/inputs_manifest.md`
2. 再按 manifest 中的 Source Inventory 和 Use Policy 读取输入资料
3. 生成“输入状态表”
4. 生成“约束提取表”
5. 生成“冲突汇总”
6. 生成“缺失输入对冻结的影响”

要求：
- 必须使用 `templates/constraint_table_template.md` 的结构
- 每条约束都要给出 `Constraint ID`
- 每条约束都要标注 `Strength`（HARD / SOFT / REFERENCE）
- 每条约束都要标注 `[CONFIRMED] / [ASSUMED] / [TBD]`
- 不能跳过冲突处理
- 不能直接进入详细设计
- 必须增加：
  - `Evidence`
  - `Decision Rationale`
  - `Chapter Binding`
  - `Impl Binding`
