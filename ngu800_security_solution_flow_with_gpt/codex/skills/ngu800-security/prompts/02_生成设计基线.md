基于 `security_workflow/01_constraints.md`，生成 `security_workflow/02_baseline.md`。

要求：
1. 不要重新回到原始材料直接写详细设计
2. 先把约束表收敛成“设计基线 / 架构裁决书”
3. 不得自行冻结架构裁决；新增 `[CONFIRMED]` baseline 必须来自用户冻结结论、accepted CR、signed-off baseline、decision_log 或官方资料
4. 缺少批准来源时，只能写 `[PROPOSED]`、`[ASSUMED]` 或 `[TBD]`
5. 设计基线必须包含：
   - Root of Trust
   - First Mutable Stage
   - First Cryptographic Verifier
   - BootROM / SEC1 / SEC2 / eHSM / Host / Board 的职责边界
   - 双算法策略
   - Manufacturing / Provisioning baseline
   - Freeze-sensitive items
   - Adopted vs Rejected decisions
6. 使用 `templates/baseline_template.md`
7. 必须包含一个 Mermaid 架构图和一个 Mermaid 时序图
8. 明确写出：当前基线是否足以进入详细设计；若只是草案，必须写明需要 ChatGPT / owner review 后才能进入冻结
