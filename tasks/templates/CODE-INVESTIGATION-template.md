# INV-SEC-xxx：<代码调查主题>

## 调查背景

- 对应Feature/Requirement：
- 对应正式详设：
- 触发问题：
- 目标仓库：
- 只读基线：引用 `manifests/repositories.yaml`，不为本任务执行Git命令。

## 调查目标

用一句话说明本次必须确认的核心代码事实。

## 需要回答的问题

1. 
2. 
3. 

## 建议检查范围

- 目录/模块：
- 关键字：
- 可能相关的文件/符号：
- 构建配置：

建议范围只用于提高搜索效率，不限制为确认调用链所必需的关联调查。

## 证据要求

- 路径、符号和紧凑行范围。
- 入口、调用链、输入/输出、错误路径和数据所有权。
- 宏/Kconfig/Makefile/链接脚本和平台/版本差异。
- 区分 `CODE_FACT`、`INFERENCE`、`GAP`、`UNKNOWN` 和 `CONFLICTING`。
- 不把函数名、注释、历史Review或目标设计直接当作代码事实。

## 输出

- 报告：`evidence/code-investigations/CE-SEC-xxx-<topic>.md`
- 需要回填的正式详设：
- 需要更新的追溯：

## 限制和停止条件

- 当前任务只读，不修改业务代码、构建文件或正式设计结论。
- 不执行Git命令，不写入两个代码仓。
- 发现版本不匹配、证据冲突或无法安全读取所需范围时停止受影响结论并报告。

## 验收

- [ ] 所有问题均有证据支持的回答或明确UNKNOWN原因。
- [ ] 关键结论具有路径、符号和调用关系。
- [ ] 条件编译、构建和平台差异已检查。
- [ ] 代码现状、目标设计和差异未混写。
- [ ] 下一步设计回填、裁决或调查动作明确。
